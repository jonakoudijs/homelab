apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rss-koudijs-lan
spec:
  secretName: rss-koudijs-lan
  commonName: rss.koudijs.lan
  dnsNames:
    - rss.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
