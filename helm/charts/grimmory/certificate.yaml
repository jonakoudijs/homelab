apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: books-koudijs-lan
spec:
  secretName: books-koudijs-lan
  commonName: books.koudijs.lan
  dnsNames:
    - books.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
