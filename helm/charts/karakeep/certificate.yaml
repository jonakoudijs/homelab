apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keep-koudijs-lan
spec:
  secretName: keep-koudijs-lan
  commonName: keep.koudijs.lan
  dnsNames:
    - keep.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
