apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: stream-music-koudijs-lan
spec:
  secretName: stream-music-koudijs-lan
  commonName: stream.music.koudijs.lan
  dnsNames:
    - stream.music.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
