apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: music-koudijs-lan
spec:
  secretName: music-koudijs-lan
  commonName: music.koudijs.lan
  dnsNames:
    - music.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
