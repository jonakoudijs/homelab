apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: koudijs-lan
spec:
  secretName: koudijs-lan
  commonName: koudijs.lan
  dnsNames:
    - koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
