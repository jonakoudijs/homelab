apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keel-koudijs-lan
spec:
  secretName: keel-koudijs-lan
  commonName: keel.koudijs.lan
  dnsNames:
    - keel.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
