---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: storage-koudijs-lan
spec:
  secretName: storage-koudijs-lan
  commonName: storage.koudijs.lan
  dnsNames:
    - storage.koudijs.lan
  issuerRef:
    group: certmanager.step.sm
    kind: StepClusterIssuer
    name: step-issuer
